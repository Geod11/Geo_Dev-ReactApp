function feast(inp, inp1) {
    const beastArray = [...inp];
    const dishArray = [...inp1];
    
  return beastArray[0] === dishArray[0] && beastArray[beast.length - 1] === beastArray[dish.length - 1]
  };
  feast(mouse, dugbng);

  function feast(beast, dish) {
    return beast[0] === dish[0] && beast[beast.length - 1] === dish[dish.length - 1]
  };
  feast(mouse, dugbng);